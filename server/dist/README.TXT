
To run the project from the command line, go to the dist folder and
type the following:

java -jar "TicTacToeServer.jar" 


Notes:
Server can run a game on one port, default port is 8901 and no args are passed. If another game is run, add a different port and add the port as an arg.
